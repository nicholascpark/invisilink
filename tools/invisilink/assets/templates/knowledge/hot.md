# Hot Cache

*Fast cache for recent vault state. Updated at the end of each librarian cycle. Any agent reads this first instead of crawling the full wiki. Stale by design — if you need authoritative current state, drill into the wiki pages themselves.*

> This is the first file every agent reads. Keep it concise. Do not duplicate content that belongs in wiki pages.

## Last Cycle

- cycle: —
- date: —
- device: —
- ingests: —
- filed-back queries: —
- lint findings: —

## Active Generalization Candidates

*Patterns being actively corroborated across ventures. Drops off this list once promoted to cross-venture or retired.*

None yet.

## Recent Contradictions

*Lint-surfaced contradictions awaiting resolution.*

None yet.

## Capability Status

*Quick reference so agents can glance here instead of reading `meta/capabilities.json` on every operation. Reflects the capabilities.json state at the last librarian run.*

- device-tagged logging: —
- packet federation: —
- lineage records: —
- fallback resilience: —
- icloud storage: —

## Fallback State

- in_fallback: false
- last_primary_uri_failure: —
- deferred_writes_pending: 0
